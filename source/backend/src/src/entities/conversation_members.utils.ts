export enum ConversationAction {
  NONE = "none",
  WRITING = "writing",
  PLAYING = "playing",
}

export enum ConversationRole {
  OWNER = "owner",
  ADMIN = "admin",
  MEMBER = "member",
  BANNED = "banned",
  MUTED = "muted",
  PENDING = "pending",
}
